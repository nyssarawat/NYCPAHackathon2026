# Workflow Evolution Log

- [process patterns] Gary steers scope live via interrupts rather than upfront specs — he rejected Explore-agent launches and fed requirements one at a time ("native zoom", "info on hover"). Build incrementally; expect the brief to grow mid-plan.
- [tool preferences] For continuation work on a known small codebase, he prefers direct file reads over spawning Explore subagents. Read the files yourself.
- [process patterns] He runs /review-plan before building and values research-backed corrections (e.g. confirming Census keyless is dead) baked into the plan.
- [quality bar] Reconcile every bucket — income join must log counties-with vs counties-without and confirm edge cases (CT old-county FIPS) explicitly.
